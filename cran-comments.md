
## Test environments
* macOS 26.1, R 4.5.0 (local)
* win-builder (devel and release)
* macOS-arm64 GitHub Actions (optional)

## R CMD check results
0 errors | 0 warnings | 0 notes

## Notes
This is the first submission of the projectLSA package.

The package provides a Shiny-based GUI for common latent structure analysis methods (LPA, LCA, LTA/IRT, EFA, CFA).
All core analysis is performed using existing CRAN packages, and no external system dependencies are required.

## Downstream dependencies
There are currently no downstream dependencies.

